services:
  - type: web
    name: birthday-wish-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health

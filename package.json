{
  "name": "birthday-wish-root",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && node index.js",
    "dev": "npx concurrently \"cd frontend && npm run dev\" \"cd backend && node index.js\"",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
